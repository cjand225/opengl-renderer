{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build",
        "type": "shell",
        "command": "cmake --preset=vcpkg -B build -DCMAKE_BUILD_TYPE=Debug -S . && cmake --build build",
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": [
          "$clang"
        ]
      }
    ]
  }
